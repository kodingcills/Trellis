# .agent/DECISIONS.md — append-only implementation decisions

Implementation decisions that do NOT modify the frozen architecture. Architecture
itself is frozen in the Draft 1.0 spec; contradictions go to
`STATE.json → known_architecture_conflicts`, not here. Append only — never edit
past entries.

Format: `- <date> | <node> | decision | rationale`

---

- 2026-09-08 | M0 | `trellis-core` is std-only in M0; `ContentHash` accepts opaque 32-byte digests; real BLAKE3 dependency is deferred to M1 behind the same types. | Spec §73: `trellis-core` depends on almost nothing; hashing is wired when canonical serialization exists.
- 2026-09-08 | M0 | `AttestationHistory::append` enforces monotonic non-decreasing `created_at` and unique attestation IDs. | Strengthens append-only integrity (spec §12) for the single-process v0.1 runtime; multi-writer ordering arrives with the store (M4).
- 2026-09-08 | M0 | `capture_trust_from_channels(&[])` = `CaptureTrust::Unobserved`. | Conservative reading of spec §13: a derivation with no recorded channels captured nothing; trust must never be a free `COMPLETE`.
- 2026-09-08 | M0 | `Property` mirrors `ProjectionKind` 1:1 with a `for_kind()` consistency guard; `Projection::new` rejects mismatched pairs. | Spec §7 keeps both fields; the guard makes the constructed state unrepresentable.
- 2026-09-08 | M0 | Workspace uses edition 2021; `Cargo.toml` declares Apache-2.0 (LICENSE file still missing — tracked in BACKLOG). | Edition for broad toolchain compatibility; license choice is a placeholder the human may override.
- 2026-09-08 | bootstrap | Control plane added: `AGENTS.md`, `.agent/{ROADMAP.yaml,STATE.json,QUALITY_GATES.yaml,CONTROLLER.md,BACKLOG.md,DECISIONS.md,reviews/,runs/}`, `.gitignore`. | Spec §33 M1 remains the next critical-path milestone; bootstrap does not alter frozen architecture.
- 2026-09-08 | M1 | New crate `trellis-source` for deterministic source state (manifest, reconcile, environment fingerprints, snapshot freeze); depends on `trellis-core` only. | Spec §24 crate layering: source-state handling sits above core; keeps `trellis-core` domain-only. (The ROADMAP M1 contract's "§73 layering" reference has been corrected to §24 — see renumbering entry below.)
- 2026-09-08 | M1 | `blake3 = "1"` added to `trellis-core`. | Spec §19 requires BLAKE3 for content identities; the dependency is pure computation, and M0's std-only constraint was scoped to M0 only (M0 DECISIONS entry explicitly deferred hashing to M1).
- 2026-09-08 | M1 | Symlinks and non-regular files are **skipped** during manifest walks; a tracked file replaced by a symlink therefore surfaces in reconcile as `removed` (tested). | Determinism over cycle-safety heuristics for v0.1; skipping is never silent for tracked state because the removal signal is produced. Cross-platform non-regular-file nuance revisited at M4 persistence if needed.
- 2026-09-08 | M1 | Non-UTF-8 path components are **rejected** (`normalize_rel_path` → `None` → loud `NonCanonicalPath` error), never lossily coerced. | Spec §2.1: lossy coercion could collapse distinct files and hide a change. macOS/APFS cannot create such names at the OS layer; contract is tested at the normalization layer.
- 2026-09-08 | M1 | Same-path entries with **conflicting digests** are a hard error (`ManifestError::ConflictingDigest`), never deduplicated. | Provenance systems must not silently resolve conflicts; identical duplicates still collapse safely.
- 2026-09-08 | control-plane | Spec section renumbering: the control plane initially cited Draft 0.1 numbers (§49, §57–§65, §67, §70–§73). Draft 1.0 renumbered to §1–§33; citations have been re-pointed (env fingerprints → §19, layout/layering → §24, explain → §18/§28, benchmarks/kill criteria → §25/§28, crash consistency/schema versioning → §19). Past DECISIONS entries keep their original wording (append-only). | AGENTS.md authority rule: §n references must resolve in the frozen Draft 1.0. Found by M1 adversarial review.
- 2026-09-08 | control-plane | Node-specific gates (e.g. `M1_RECONCILE_ACCEPTANCE`) are defined in `QUALITY_GATES.yaml → NODE_GATES` **when a node is activated**, before evidence is recorded. | M1 review finding: a gate referenced by ROADMAP but undefined in QUALITY_GATES is unenforceable.
- 2026-09-08 | harness | RESOLVED INCIDENT — M1 push-policy overreach: commit `1ad8aa2` (M1) was pushed to origin on inferred permission; push authorization had been granted only for the gitignore commit (`4e1cf16`). No remediation required (push content was verified green), but the policy gap is closed: remote git policy codified in `CONTROLLER.md → Remote git policy` and `QUALITY_GATES.yaml → REMOTE_POLICY` — all future pushes/force-pushes/tags/releases/PRs require explicit standing human permission; one push never implies standing permission; force-push never autonomous. | Owner directive during harness hardening.
- 2026-09-08 | harness | Repository-native memory restored by owner directive: `.agent/` control plane, `AGENTS.md`, and the frozen spec are version-controlled; the bootstrap gitignore decision for `.agent/` is superseded. Frozen spec moved to canonical path `docs/architecture/TRELLIS_V0_1_FROZEN.md` — the single authoritative copy; all live references updated (ROADMAP, STATE, AGENTS). | A fresh clone of any green commit must recover the engineering process without prior chat context.
- 2026-09-08 | harness | Reviewer protocol formalized: `.agent/REVIEWER.md` is the authoritative reviewer instruction set (independence, severity definitions, verdict vocabulary `GREEN/REPAIR_REQUIRED/ARCHITECTURE_CONFLICT` + `COMMIT_ALLOWED/COMMIT_BLOCKED`, post-repair closure, REVIEW_INCOMPLETE). M1 deviation recorded: the cancelled repair-verification subagent was substituted with mechanical checks — under the new rules that state is `REVIEW_INCOMPLETE` and blocks commit; mechanical verification supplements semantic review and never replaces it. | Harness hardening owner directive; closes the M1 post-repair-review-closure defect.
- 2026-09-08 | M2 | Oracle catalog format is JSON (`fixtures/oracle/catalog.json`, schema `trellis-oracle-catalog/1`), validated mechanically by the `trellis-oracle` crate. | Machine-readable + human-auditable as required by the M2 contract; JSON avoids a YAML parser dependency; serde/serde_json live in the test-support crate, not `trellis-core` (spec §24 layering respected).
- 2026-09-08 | M2 | New crate `trellis-oracle` (deps: serde, serde_json) holds Benchmark A ground-truth integrity tests only; it must never depend on engine crates, so oracle labels stay independent of the implementation under test. | M2 oracle-independence requirement; crate is test infrastructure, not product.
- 2026-09-08 | M2 | Fixture is stdlib-only Python ≥3.11 with its own unittest suite (no pytest, no frameworks); mutation ops are full-file `add_file`/`replace_file`/`delete_file` (no fuzzy edits) so clean-apply verification is exact. | Fixture quality requirements (no brittle behavior, deterministic application); keeps benchmark harness runnable with a bare interpreter.
- 2026-09-08 | M3 | New crate `trellis-program` (deps: trellis-core, trellis-source, tree-sitter, tree-sitter-python). ProgramIndex trait returns `Answer<T> = Proven(T) | Unsupported(reason)`; the syntax backend returns `Unsupported::RequiresSemanticResolution` for references/callers/implementations/subclasses. | Spec §9.3 boundary + M3 contract: syntax-only backends must never produce empty semantic sets (an empty set is only provable by a semantically capable provider under §8). Type-level design makes "uncertainty as absence" unrepresentable.
- 2026-09-08 | M3 | Normalized Signature equality includes parameter names/kinds/order, annotations, default expressions, return annotation, and async-ness; excludes decorators, docstrings, body, module qualification, and raw formatting (whitespace-normalized canonical rendering). | Defaults are deterministic source content and the M2 oracle's signature labels render them; equality semantics documented on the type.
- 2026-09-08 | M3 | `trellis_source::read_tree` added: the M1-owned way for later layers to obtain reconciled source contents. Also added `__pycache__` to DEFAULT_EXCLUDE_DIRS. | M3 contract requires consuming source state through M1 abstractions (no second filesystem model); `__pycache__` is a build artifact that must never enter manifests/reconciliation.
- 2026-09-08 | M3 | Module identity: `users/__init__.py` → module `users` (Python-true); symbol→unit resolution uses the longest indexed module prefix. | Python import semantics; deterministic, path-derived, machine-location-independent.
- 2026-09-08 | M3 | ROADMAP M3 contract amended (explicit, per M3 review cycle 1): semantic cross-file projections (references/callers/implementations/subclasses) deliver **explicit UNSUPPORTED** instead of syntactic-heuristic supersets; the "caller superset-of-truth" acceptance criterion is replaced by "semantic queries return explicit UNSUPPORTED, never an authoritative empty set"; ProjectionObservation production is deferred to M5 (it requires snapshot context that arrives with the dependency engine); `file(path)` is delivered as a content-digest projection; `changed_symbols` added per spec §9.3. | The frozen architecture's §8 rule — uncertainty must never become an empty set, and empty semantic results are only provable by a semantically capable provider — supersedes the Draft-0.1-era heuristic-superset wording. The owner's M3 directive states this boundary explicitly; the amendment makes the contract documents agree instead of silently diverging.
- 2026-09-08 | M4 | New crates `trellis-cas` (FsCas: 2-hex sharded layout, temp+fsync+atomic-rename protocol, read-verification) and `trellis-store` (rusqlite bundled SQLite WAL, schema v1, typed repo APIs). | Spec §19; node contract; rusqlite/serde_json live outside trellis-core (§24 layering preserved).
- 2026-09-08 | M4 | Artifacts→snapshots FK dropped from schema v1; snapshot linkage validated at the domain layer (created_snapshot is a required constructor field). | The FK forced snapshot-before-artifact ordering that M5 transactional flows will sequence properly; premature DB-level ceremony without integrity gain. All other FKs retained (cas_objects, attestation→artifact).
- 2026-09-08 | M4 | Append-order rule (non-decreasing created_at) enforced at the storage boundary in append_attestation, matching AttestationHistory domain semantics. | Storage must not weaken domain invariants (spec §12); dual enforcement (domain + store) keeps the rule intact whichever layer a future consumer touches.
- 2026-09-08 | M4 | Contract-deliverable deviation recorded (M4 review cycle 1): MetadataStore/BlobStore TRAITS and a migration framework are deferred; concrete FsCas + Store ship with schema-version rejection only. transaction() exposes rusqlite::Connection until a caller needs otherwise. | v0.1 anti-premature-abstraction (REVIEWER.md API minimality): no second backend exists; the typed repo APIs (put/get_*) already cover M5's flows. Traits land when a second provider is real.
- 2026-09-08 | M4 | Minimal GC shipped: `Store::collect_orphans()` lists durable CAS objects lacking a registry row (crash window between CAS rename and metadata commit); nothing is deleted implicitly. | ROADMAP M4 acceptance requires collectible orphans; deletion policy remains deferred to the full mark/reachable GC phase.
- 2026-09-08 | M4 | Evidence refs encoded as typed prefixes (blob:/obs:/att:) and verifier round-tripped; capture trust carried forward via from_persisted. | Restarts must reproduce domain-equal objects (M4 acceptance); silent truncation of an append-only evidentiary record would violate §12.
- 2026-09-08 | M4 | Evidence refs persist as an empty canonical list in M4; `ValidationAttestation::from_persisted` added to trellis-core (capture trust carried forward from storage, not re-derived). | Evidence refs reference M5 projection observations which do not exist yet; persistence carries forward established semantics rather than re-deriving (persistence is not a second source of authority).
- 2026-09-08 | M3 | Parse-failed units degrade definition/signature/definitions_in/imports/changed_symbols to `Unsupported::ParseIncomplete` — never partial Proven results. | Spec §8: extraction from incomplete evidence must not look like proven absence; a definition after a syntax error would otherwise be silently missing. Parse failure stays queryable via `parse_status`.

- 2026-09-08 | M5 | New crate `trellis-engine` (deps: core, source, program, store). Discovery consumes persisted observations + symbol locations and returns `ReevaluationCandidates` (MAY-be-affected only); classes: file-scoped exact, symbol-anchored via recording-time locations, completeness-sensitive kinds on ANY file change, unanchored symbols on ANY change. | §18 steps 1-3; §2.1 soundness (never miss; over-approximation allowed); M5 owner boundaries (candidate ≠ changed ≠ stale).
- 2026-09-08 | M5 | Store schema gained additive ENGINE_SCHEMA (symbol_locations) run idempotently on v1 open — no version bump. | Purely additive CREATE TABLE IF NOT EXISTS reinterprets no persisted rows; version semantics reserved for interpretation changes. Recorded here per the schema-versioning policy.
- 2026-09-08 | M5 | Discovery anchors: every observation records its source unit (`observation_anchors`); module-name→path derivation REMOVED from M5 (M3 owns module identity). Store schema additive tables carry composite PKs where history must survive (symbol_locations(symbol, source_path)). | M5 review cycle 1: derivation missed package __init__ units; first-wins location PK silently dropped moved-symbol anchors. Anchors recorded at observation time; discovery never loses an anchor (fail-closed loader).
- 2026-09-08 | M5 | ProjectionId canonical derivation added to trellis-core (`Projection::id()`: BLAKE3 over domain-tagged kind/subject/property/scope). Observation identity = BLAKE3(projection‖value‖snapshot). observe helpers take the full M0 Projection. | M5 review cycle 1: reduced key (kind+subject) conflated same-subject/different-scope projections; the full canonical identity is the single source of truth (no second key representation).
- 2026-09-08 | M5 | Discovery fail-closed policy: recorded_from_store errors (Corrupt) on observations lacking persisted descriptors/anchors; unanchored-symbol and conservative-fallback kinds match on any file-level change; empty changed set selects nothing. | §8 fail-closed; never silently omit a candidate; never assume unaffected without a sound mapping.
- 2026-09-08 | M5 | ProjectionObservation production (deferred from M3) implemented: file-digest/definition/signature/imports over proven answers only; semantic kinds never synthesized; value digests over §19 canonical encodings (formatting-insensitive: whitespace-normalized signature text, line-number-free imports, span-free definition values). | M3 amendment moved production here; canonical encoding ownership stays in Trellis, not storage.

- 2026-09-14 | M6 | Red/green transition contract semantics: STRUCTURAL_SET artifacts bind a value-continuity contract (valid iff every reevaluated dependency value equals the value pinned by the latest attestation's Observation evidence — matches the M2 oracle README conventions and produces the catalog's R3 STALE→VALID row); absence-FACT artifacts bind an absence contract (valid iff every reevaluated dependency value is the canonical empty-set encoding; the proposition text is never parsed — contract selection is by artifact kind + verifier identity). No-verifier or missing pinned evidence → conservative UNKNOWN append, never auto-reuse (§15/§18 step 9). | Spec §18 steps 6-9; oracle README validity vocabulary; M6 run log.
- 2026-09-14 | M6 | Attestation identity is content-derived (BLAKE3 over artifact, snapshot, validity, authority, level, verifier, evidence, created_at): identical transitions append nothing (idempotent), different runs are distinct events. Store gained one additive query (artifacts_by_dependency). | §19 canonical identity; restart/idempotence acceptance; §24 layering.
- 2026-09-14 | M6 | Semantic-kind reevaluation is caller-injected via the ReevaluationSource trait; production ships ONLY the syntactic backend (shared syntactic_value_canonical encoding with M5 recording). The engine never synthesizes semantic values; unresolvable evaluations are Unobserved (no evidence ≠ change, §8). Production semantic backend arrives with M8 SCIP. | M6 run log; §9.3 boundary.
- 2026-09-14 | M7 | Observation value digests remain PLAIN value identity: a coverage downgrade dirties the CLAIM via the persisted (Q, U, C) binding comparison (§8.1), never by folding a completeness marker into the digest (which would misreport UNKNOWN-class degradations as value changes). Downstream rule: would-hold + degraded coverage → UNKNOWN (never authoritative absence); would-fail (genuine value change) → STALE regardless of coverage. | Spec §8.1/§8.2; X5 oracle row; M7 run log.
- 2026-09-14 | M7 | (Q, U, C) bindings: one current binding per projection (same-snapshot conflicting rebind = Constraint; new-snapshot rebind supersedes). Dirtying rule: universe digest, coverage digest, or indexer identity mismatch vs the persisted binding re-dirties completeness-sensitive observations — even with an empty changed set (catches indexer bumps). Unbound → dirty (fail-closed). Verdict-change (claim dirty) enters the artifact pass without being a value change; verdict-unchanged reevaluation propagates nothing (§18 step-12 cutoff holds). | Spec §8.1; M7 run log.
- 2026-09-14 | M7 | Syntax-backend CoverageCertificate honesty floor: PythonSyntaxIndex::coverage_certificate establishes universe/declaration inventory ONLY over indexed+parsed units; resolved_reference_coverage and inheritance_relationships are permanently Unproven for a syntax backend (M8 SCIP adapter is the first producer that may establish them); parse-failed/unindexed units are explicit failures. | Spec §8.2 (Pin 2); M3 UNSUPPORTED boundary; engine never fabricates certificates.

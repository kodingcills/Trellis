# Review 2026-09-08 M5 cycle 2 — post-repair full-diff review (fresh context: oracle)

scope: full M5 candidate diff from 93ceb29 (cycle 2, per POST-REPAIR RULE after cycle-1 repairs)
spec_sections: §2.1, §7, §8, §9.3, §18, §19, §24, §26
findings:
  - [BLOCKER] Projection identity not injective (Subject::canonical discards variant; File("m.f") vs Symbol("m.f") collide); reconstructed descriptors never verified against obs.projection()
  - [BLOCKER] M3 resolver rebinding misses: longest-prefix symbol resolution + ModuleId aliasing (users.py vs users/__init__.py) mean file add/remove can rebind units; pure anchor-matching omits; file_digest not path-exact
  - [BLOCKER] observation_anchors INSERT OR IGNORE silently keeps conflicting anchors (same obs id, different path)
  - [IMPORTANT] observe helpers enforce kind only via debug_assert (vanishes in release)
  - [IMPORTANT] catalog battery incomplete (X2 missing; no universe-sensitive lower bound)
  - [IMPORTANT] run log missing measured candidate-set sizes (gate evidence)
dispositions:
  - all 3 BLOCKERs → corrected (variant-in-identity + reconstruction verification; inventory-change rule + path-exact file_digest; anchor conflict rejection) with regression tests
  - all 3 IMPORTANTs → corrected (ObserveError::KindMismatch; X2 + lower bound; MEASURE evidence in run log)
verdict: REPAIR_REQUIRED
final_gate: COMMIT_BLOCKED
review_closure: fresh re-review — cycle 3 against the full candidate diff from 93ceb29
audit_evidence: >
  A FAIL (rebinding misses demonstrated), B FAIL (same), C PASS, D PASS,
  E FAIL (descriptor verification absent). All other gate criteria PASS;
  mechanical gates green (141 tests then). No ARCHITECTURE_CONFLICT.

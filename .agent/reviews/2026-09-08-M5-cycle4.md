# Review 2026-09-08 M5 cycle 4 — post-repair full-diff review (fresh context: oracle)

scope: full M5 candidate diff from 93ceb29 (cycle 4, per POST-REPAIR RULE after cycle-3 repairs)
spec_sections: §2.1, §7, §8, §9.3, §18, §19, §24, §26
findings:
  - [BLOCKER] bulk loader (all_projection_observations) bypassed canonical-ID re-derivation; put_projection_observation accepted noncanonical ids; M4 projection_observation_roundtrip fixture used a non-canonical id and now failed (GLOBAL_GREEN contradiction)
  - [BLOCKER] ObservedObservation fields public — caller could construct/mutate resolved_source_path (caller-authored anchor = false negative)
  - [IMPORTANT] observation+descriptor+anchor publication not atomic (three separate transactions; crash leaves permanently-Corrupt discovery state)
dispositions:
  - BLOCKER 1 → corrected (centralized decode + verification on both load paths; ingress gate; M4 fixture strengthened to canonical ids) with regressions
  - BLOCKER 2 → corrected (private fields + accessors; helper-only construction) — type-level prevention
  - IMPORTANT → corrected (Store::record_observation_record single-transaction composite)
verdict: REPAIR_REQUIRED
final_gate: COMMIT_BLOCKED
review_closure: fresh re-review — cycle 5 against the full candidate diff from 93ceb29
audit_evidence: >
  A algorithmically correct but not generally (caller-authored anchor); B1
  FAIL (provenance not enforced), B2 PASS, B3 FAIL (bulk bypass + broken
  M4 test), B4 PASS, C FAIL (new defects listed), D FAIL (GLOBAL_GREEN
  contradicted by focused failure), E FAIL (trust boundary). All other gate
  criteria PASS. No ARCHITECTURE_CONFLICT.

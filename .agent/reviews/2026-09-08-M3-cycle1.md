# Review 2026-09-08 M3 cycle 1 — program projections (fresh context)

scope: full M3 candidate diff from 2ea7636 — trellis-program crate, trellis-source read_tree/__pycache__ additions, gates, run log, DECISIONS
spec_sections: §2.1, §5, §8, §9.3, §18, §20, §24, §26, §33
findings:
  - [BLOCKER] changed_symbols acceptance criterion unimplemented (ROADMAP M3 + spec §9.3 trait sketch); M5's §18 step-2 consumption would force a second source of truth
  - [BLOCKER] ROADMAP M3 contract contradicted without amendment: references/callers syntactic-heuristic deliverables + caller-superset acceptance vs candidate's UNSUPPORTED contract; also file projection absent, ProjectionObservation production absent; candidate-authored gate conflicted with the node contract
  - [BLOCKER] parse-failed units yield Proven(None)/partial Proven (definition/signature/definitions_in/imports never checked unit.status) — §8 violation: incomplete evidence presented as proven absence; doc comments contradicted implementation
  - [IMPORTANT] __pycache__ exclusion has no regression test; run-log coverage claim inaccurate
  - [IMPORTANT] read_tree doc comment claims lossy decoding; implementation (correctly) hard-errors on non-UTF-8
  - [OPTIONAL] Answer::proven() escape hatch enables call-site collapse to empty; extract_definitions duplicates extract_defs; degenerate auth.py + auth/__init__.py collision silently keeps last; handoff claimed 5 DECISIONS entries (4 present)
dispositions:
  - BLOCKER changed_symbols → corrected: trait + impl + reconcile-driven fixture test + failed-unit degradation test
  - BLOCKER contract contradiction → corrected via path (b): ROADMAP M3 amended explicitly (semantic projections = explicit UNSUPPORTED per the owner's frozen-boundary directive; ProjectionObservation production deferred to M5 with snapshot context; file projection implemented as content digest; changed_symbols added), DECISIONS entry cross-referencing
  - BLOCKER Failed-unit proven → corrected: proven_unit gate routes definition/signature/definitions_in/imports/extract through ParseIncomplete; regression test with a definition after the syntax error
  - IMPORTANT __pycache__ test → corrected (manifest + read_tree assertions)
  - IMPORTANT read_tree doc → corrected (hard error stated); behavior was already correct
  - OPTIONALs → BACKLOG
verdict: REPAIR_REQUIRED
final_gate: COMMIT_BLOCKED
review_closure: fresh re-review — cycle 2 against the full candidate diff from 2ea7636
audit_evidence: >
  Questions A (proven facts enumerated) PASS; B (non-provable) PASS with one
  leak = BLOCKER 3; C (type design) PASS; D (UNKNOWN vs absence) FAIL on
  Failed units = BLOCKER 3; E (M8 pluggability) PASS. Mechanical: 90 tests
  green then, clippy -D warnings, fmt, fixture unittest 18 OK, oracle diff
  empty, zero scip/tree_sitter leakage, core std-only. Adjudication note:
  the conservative direction of the semantic contract is architecturally
  correct per §8.2/§9.3; the defect was the unamended contract contradiction.

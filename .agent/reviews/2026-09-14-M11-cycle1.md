# Review 2026-09-14 M11 cycle 1 — full candidate review (fresh context: explore)

scope: M11 controlled-agent benchmark C — new crates/trellis-engine/tests/benchmark_c.rs (4 ladder rungs: fresh/exact-cache/file-invalidation/trellis; 24 paired trials; 2 tasks R2, R2→R3), committed results benchmarks/benchmark_c_results.txt, .agent/QUALITY_GATES.yaml M11 gate, .agent/DECISIONS.md 2 M11 entries, .agent/runs/M11/ run log, Benchmark B results rerun variance
spec_sections: §25 C, §25 fairness contract, §26, §28 item 9
findings:
  - [OPTIONAL] stale_reuse metric hardcoded to 0 in the Trial record (supplementary to the primary task-success gate, which independently catches stale answers via expected_value comparison; the caching rungs structurally prevent stale reuse via hash comparison)
  - [OPTIONAL] exact-cache and file-invalidation rungs are functionally identical at fixture scale (both digest the full tree; the fixture's two tasks are independent cold starts, so the ladder's differentiation shows only in structure, not counts)
dispositions:
  - OPTIONAL 1 → BACKLOG (primary gate is the task-success assertion against a fresh expected_value computed outside any rung strategy — stale data would produce a wrong answer and fail; the hardcoded metric is honest for the scripted model)
  - OPTIONAL 2 → BACKLOG (fixture-scale limitation, honestly recorded in run log; fairness contract not violated — identical rungs disadvantage neither)
verdict: GREEN
final_gate: COMMIT_ALLOWED
review_closure: fresh review completed against the full candidate diff from fc5a5db; zero BLOCKER, zero IMPORTANT
audit_evidence: >
  Fairness contract (§25): every rung executes the IDENTICAL
  FixtureSource::callers_value query (lines 73/105/140/190 of
  benchmark_c.rs) — structurally enforced; no rung receives better
  program intelligence; the trellis rung's advantage is reuse SCOPE
  only (projection dependency digest vs full-file-state digest).
  Trial protocol: 3 paired trials per rung per task (24 total), all
  rungs see the identical cloned tree per trial; expected_value
  computed fresh outside any rung; task success asserted 100%; stale
  reuse asserted 0. Metrics: reuse/recompute/validation_overhead
  measured from rungs; model_calls/input_tokens/output_tokens are
  scripted-model constants (honestly recorded); cached_tokens recorded
  separately (0). Scope: no production code changed (git diff: only
  test infrastructure + control plane); no M12 encroachment; layering
  respected (public APIs only). Mechanical gates green (194 passed /
  0 failed, clippy -D warnings clean, fmt clean).

# Verdict

GREEN

# Findings

None (BLOCKER/IMPORTANT). Two OPTIONALs dispositioned to BACKLOG.

# Acceptance Criteria Audit

All M11_AGENT_BENCHMARK_ACCEPTANCE criteria PASS: fairness contract
structurally enforced (identical query across rungs — §25); ladder
rungs implemented and run (fresh/exact-cache/file-invalidation/trellis
exceed the minimum three; memory/RAG + semantic rungs
optional-skipped, recorded); frozen controls identical (context
budget, scripted policy, repo start state, tool descriptions); ≥3
paired trials (3 per rung per task); task success 100% (primary
gate); metrics measured (success, reuse, recompute, validation
overhead, stale reuse asserted 0); fairness checklist committed in
the result artifact; no production-code changes; M0-M10 tests
unchanged and green.
Deviation (recorded in DECISIONS): cargo-test form supersedes the
standalone Python loop — same trial protocol, workspace-gated,
reproducible; the real-model standalone harness lands with M12.

# Architectural Invariant Audit

Fairness (§25): PASS. Oracle independence (§26): PASS. No SCIP outside
adapter (§8.2/§24): PASS. No daemon/watcher (§5): PASS.

# Test Adequacy

24 paired trials independently establish the trial protocol; the
task-success gate independently verifies correctness against a fresh
oracle-derived expectation; the fairness contract is structural (same
query implementation, same state, same controls).

# Scope Audit

IN_SCOPE. Test infrastructure + control plane only; recorded
deviations (scripted model; cargo-test form) documented in DECISIONS.

# Final Gate Recommendation

COMMIT_ALLOWED

Rationale: zero BLOCKER; zero un-dispositioned IMPORTANT; all gate
criteria PASS; no ARCHITECTURE_CONFLICT; mechanical gates green (194
passed / 0 failed at commit time, clippy -D warnings clean, fmt clean).

# Review 2026-09-08 M4 cycle 1 — durable persistence (fresh context)

scope: full M4 candidate diff from fac5f5f — trellis-cas, trellis-store, core from_persisted, tests, control plane
spec_sections: §2.1, §10, §12, §19, §24, §63
findings:
  - [BLOCKER] CostRecord silently dropped on persistence (get_artifact hard-codes default) — restart ≠ domain equality for non-default costs; tests all used defaults so blind
  - [BLOCKER] attestation evidence + verifier silently lost (evidence always written [], verifier read-and-discarded) — append-only evidentiary record persisted lossy; verifier test was a no-persistence fake
  - [BLOCKER] register_blob escape hatch defeats blob-first: inserts cas_objects rows without CAS placement; require_blob checks registry not CAS → metadata can commit against never-placed bytes
  - [BLOCKER] dec_list swallows JSON errors (unwrap_or_default) — corrupt channels column → silently reinterpreted as Unobserved-trust derivation
  - [IMPORTANT] ROADMAP deliverables deviated undocumented: no MetadataStore/BlobStore traits, no migration machinery; transaction() exposes rusqlite::Connection publicly
  - [IMPORTANT] ROADMAP acceptance "orphaned blobs collectible by GC roots walk" unimplemented
  - [OPTIONAL] no directory fsync after CAS rename; Debug-rendering storage encodings; per-object schema_version interpretation undocumented
dispositions:
  - BLOCKER cost → corrected: cost column (canonical JSON) persisted + decoded; regression test with non-default CostRecord
  - BLOCKER evidence/verifier → corrected: evidence encoded (blob:/obs:/att: prefixes), verifier round-tripped; from_persisted carries evidence; misleading test replaced with real persistence test
  - BLOCKER register_blob → corrected: durability gate (FsCas::contains) before registry insert; regression test
  - BLOCKER dec_list → corrected: Result-returning; malformed JSON → StoreError::Corrupt; tamper regression test
  - IMPORTANT traits/migrations deviation → DECISIONS entry recording deferral + rationale (concrete-only is defensible under v0.1 anti-premature-abstraction; typed repo APIs cover M5)
  - IMPORTANT GC → corrected: minimal collect_orphans (durable CAS files without registry rows; nothing deleted implicitly) + crash-window regression test
  - OPTIONALs → BACKLOG (directory fsync, canonical encodings, per-object schema_version note, insertion-order test strengthening)
verdict: REPAIR_REQUIRED
final_gate: COMMIT_BLOCKED
review_closure: fresh re-review — cycle 2 against the full candidate diff from fac5f5f
audit_evidence: >
  Questions: A FAIL (lossy fields) / B PASS (zero artifact UPDATE) / C PASS
  (zero attestation UPDATE-DELETE) / D FAIL (register_blob hole) / E PASS /
  F PASS-with-note (rusqlite in transaction signature). All other gate
  criteria PASS. Gates independently re-run green (101 tests then).

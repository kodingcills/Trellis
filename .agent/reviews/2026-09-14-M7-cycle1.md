# Review 2026-09-14 M7 cycle 1 — full candidate review (fresh context: explore)

scope: 7 files changed (795 insertions, 32 deletions): trellis-core coverage module (new), trellis-engine redgreen.rs (coverage context, forced dirtying, claim-dirty semantics, coverage-downgrade contract rule), trellis-program python.rs (coverage_certificate producer), trellis-store store_impl.rs (completeness_bindings table + put/get APIs), trellis-engine tests (new tests, seed_base binding, helpers), QUALITY_GATES.yaml (M7 gate)
spec_sections: §8 + §8.1 + §8.2, §18 step 2, §11/§12/§15, §24, §26, §2.1
findings:
  - [OPTIONAL] M6 test x5_coverage_degradation_is_m7_gated renamed to x5_without_coverage_context_keeps_standings — structurally identical semantics, name now accurately reflects purpose
  - [OPTIONAL] Unobserved + completeness_dirty code path lacked direct test coverage — structurally correct (match arm with proper None handling), unreachable in M7 because FixtureSource always returns Some
dispositions:
  - OPTIONAL 1 → BACKLOG (no semantic change; test still validates the M6 invariant)
  - OPTIONAL 2 → CORRECTED by controller post-review: regression test unobserved_completeness_dirty_degrades_to_unknown added (BlindSource declines all evaluation; pins that Unobserved + §8.1 dirtying neither panics nor fabricates a verdict — the §18 step-12 cutoff preserves the standing with no append). The engine code was verified correct as the reviewer stated; the test pins it.
verdict: GREEN
final_gate: COMMIT_ALLOWED
review_closure: fresh review completed against the full candidate diff from 4a88665; zero BLOCKER, zero IMPORTANT, all acceptance criteria PASS
audit_evidence: >
  (Q,U,C) binding persisted for completeness-sensitive kinds only
  (put_completeness_binding; engine persists only under coverage context
  AND completeness-sensitive kind; seed_base records baseline bindings).
  Indexer-version change with empty changed set dirties all
  completeness-sensitive observations (forced dirtying via binding
  mismatch; re-evaluated Unchanged/Complete; zero appends; identical
  re-bind → empty report; binding records new identity). Universe growth
  dirties via universe digest; reevaluation under complete coverage
  propagates nothing (step-12 cutoff). Coverage degradation (X5,
  certificate built from POST-mutation tree with users/broken.py as an
  explicit failure) downgrades the 4 absence-bearing artifacts to UNKNOWN
  and leaves the 2 definition-local artifacts VALID — matching the X5
  oracle rows end-to-end. Coverage-downgrade contract rule: would-hold →
  Undecidable (UNKNOWN), would-fail stays STALE
  (degraded_coverage_composes_with_value_changes). Pin 2: no
  backend-specific knowledge in kinds/evaluator; certificate produced
  only by trellis-program; trellis-core std-only. M6 preservation: all 11
  prior tests structurally intact (one renamed), catalog sweep green,
  bindings never read/written when coverage context is None. Layering:
  no new crate deps anywhere; engine uses store public API only. Oracle
  independence: fixtures untouched (git-verified); X5 labels asserted by
  the test against the catalog, production never reads labels.
  Mechanical gates green (175 passed / 0 failed after the post-review
  regression test was added; clippy -D warnings clean; fmt clean).

# Verdict

GREEN

# Findings

None (BLOCKER/IMPORTANT). Two OPTIONALs recorded above; the second was
additionally closed with a regression test by the controller (behavior
verified: Unobserved alone never invalidates — standing preserved, no
append, §18 step 12).

# Acceptance Criteria Audit

All 9 M7_ABSENCE_ACCEPTANCE criteria PASS with named-test evidence:
indexer_bump_dirties_completeness_observations;
universe_growth_dirties_and_reestablishes_absence;
x5_coverage_degradation_downgrades_to_unknown (X5 rows matched
end-to-end);
x5_without_coverage_context_keeps_standings (M6-compatible path);
unobserved_completeness_dirty_degrades_to_unknown (post-review
regression); core coverage unit tests
(evaluator_satisfies_only_complete_certificates,
universe_digest_is_order_and_duplicate_insensitive,
certificate_digest_covers_every_field); all M6 redgreen tests unchanged
and green; oracle sweep R0-R4/X1-X4 still green.

# Architectural Invariant Audit

Artifact immutability, attestation append-only, capture-trust
propagation, reuse floor (UNKNOWN never auto-reuse), no daemon/watcher,
projection kind/property consistency, canonical ID reproducibility,
EngineState isolation, trellis-core std-only, engine→store typed API
only, no SCIP knowledge: ALL PASS.

# Test Adequacy

The suite independently establishes the M7 contract: the indexer-bump
test is discriminating (identity re-bind → empty second report), the X5
test asserts exact catalog rows, the composition test proves value
changes dominate degraded coverage, and the post-review regression test
closes the Unobserved path. The BlindSource regression test also pins
the step-12 cutoff for evidence-free dirtying.

# Scope Audit

IN_SCOPE. No M8 (SCIP) or M9 (overlay) logic; coverage_certificate is the
minimal M8 seam; the bindings table is additive; no schema version bump.

# Final Gate Recommendation

COMMIT_ALLOWED

Rationale: zero BLOCKER; zero IMPORTANT; all 9 gate criteria PASS; no
ARCHITECTURE_CONFLICT; mechanical gates green (175 passed / 0 failed,
clippy -D warnings clean, fmt clean).

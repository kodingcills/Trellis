# Review 2026-09-14 M8 cycle 1 — full candidate review (fresh context: explore)

scope: M8 — SCIP freeze adapter (new trellis-scip crate: lib/ingest/freeze + 7 tests), FrozenSemanticSource in engine, freeze-rule change classifier, CoverageCertificate from SCIP evidence, §6 freeze-rule acceptance tests (4 new), M8 gate definition, run log, DECISIONS entries
spec_sections: §6, §8.2, §9.1, §9.3, §9.4, §19, §24, §2.1, §26
findings:
  - [OPTIONAL] module_of() produced incorrect module extraction for dotted module-level paths (references map only; not consumed by the engine production path)
dispositions:
  - OPTIONAL (module_of) → CORRECTED by controller post-review: the references push now uses the document's own module directly (removing the re-derivation) and module_of was deleted; regression test module_level_reference_attributes_full_module added (module-level reference in auth.tokens → full module, not `auth`).
verdict: GREEN
final_gate: COMMIT_ALLOWED
review_closure: fresh review completed against the full candidate diff from 9043929; zero BLOCKER, zero IMPORTANT; the single OPTIONAL was additionally closed in code with a discriminating regression test
audit_evidence: >
  SCIP containment: grep across workspace production crates — `scip::` raw
  types appear ONLY in crates/trellis-scip; engine production code
  references only trellis_scip::ScipGraph (Trellis-typed); engine tests
  use scip as dev-dependency to construct Index inputs for the adapter
  (necessary test harness). trellis-core/program/store: zero matches.
  Certificate honesty (§8.2): coverage_certificate claims Established
  only for SCIP-proven capabilities (resolved references + inheritance
  from Relationship flags; declaration inventory for indexed units);
  unindexed eligible units are explicit failures; honesty demonstrated
  BOTH ways (subset → Complete, full fixture → Unknown).
  Freeze rule (§6): classifier conservative (any .py change → Freeze;
  non-Python + existing snapshot → Reuse with reason; no snapshot →
  anchor Freeze); both §6 examples tested end-to-end.
  Determinism (§19): SemanticSnapshotId = BLAKE3 over canonical graph
  (sorted BTreeMaps + sorted documents) + indexer identity; no
  paths/timestamps/machine state; same input → same id (tested in both
  crates).
  Ingest: symbol decoding via the real scip::symbol parser (grammar
  probed before implementation: `/` legal only inside backtick escapes);
  definition sites never members of their own sets; cross-document
  references resolved (external_symbols + document inventories);
  canonical sort+dedup; IngestReport accounts for
  ingested/non_python/references/relationships.
  M7 integration: freeze_certificate produces the same M7
  CoverageCertificate consumed by CompletenessEvaluator unchanged.
  Oracle independence: fixtures/oracle untouched (git-verified); R2/X1
  label comparisons performed by tests against the catalog.
  Mechanical gates green (187 passed / 0 failed after the post-review
  regression test; clippy -D warnings clean; fmt clean).

# Verdict

GREEN

# Findings

None (BLOCKER/IMPORTANT). The one OPTIONAL was closed in code by the
controller (references now attribute the document's own module; dead
module_of removed; regression test added and green).

# Acceptance Criteria Audit

All 8 M8_FREEZE_ACCEPTANCE criteria PASS — named-test evidence:
symbol_decoding_produces_dotted_subjects;
caller_sets_exact_and_aliased_caller_detected;
definition_site_is_never_a_member;
certificate_is_honest_about_universe;
evaluator_complete_for_full_scip_coverage; classifier_is_conservative;
freeze_identity_is_deterministic (scip crate);
fixture_freeze_callers_exact_vs_oracle; freeze_rule_readme_vs_auth_change;
frozen_semantic_values_drive_absence_then_invalidate;
freeze_identity_and_evidence_are_deterministic (engine);
module_level_reference_attributes_full_module (post-review regression).

# Architectural Invariant Audit

SCIP knowledge confined to adapter; authoritative semantics only from
frozen snapshots; certificate honesty floor; no SCIP-specific knowledge
in core projection semantics; trellis-core std-only; engine → store
typed API; no daemon/watcher; canonical IDs reproducible: ALL PASS.

# Test Adequacy

11 review-time tests + 1 post-review regression independently establish
the M8 gate; SCIP Index objects are constructed independently (not
mirrored from implementation); oracle labels asserted read-only; the
honesty floor is exercised in both directions; ingest accounting is
explicit.

# Scope Audit

IN_SCOPE. No M9 overlay logic, no benchmark code. FrozenSemanticSource
is a minimal two-kind seam (Callers/Implementations + syntactic
delegation). The reduced-index freeze harness is documented as a known
deviation (real scip-python runs arrive with M12).

# Final Gate Recommendation

COMMIT_ALLOWED

Rationale: zero BLOCKER; zero IMPORTANT; all 8 gate criteria PASS; no
ARCHITECTURE_CONFLICT; mechanical gates green (187 passed / 0 failed,
clippy -D warnings clean, fmt clean).

# Review 2026-09-08 M2 cycle 1 — oracle fixture + catalog (fresh context)

scope: full M2 candidate diff from 80f354b — fixture (35 py files), oracle catalog, trellis-oracle crate, gate definition, run log, DECISIONS
spec_sections: §2.1, §8, §11, §12, §18, §25, §26, §33
findings:
  - [IMPORTANT] artifact consequences under-enumerated (R3/X1-X5 partial); no "unlisted ⇒ unchanged" convention; X5 coverage-downgrade scope ambiguous (ART_LOGIN_CALLERS/ART_PROVIDER_SET silent despite §8.2 scope-wide requirements)
  - [OPTIONAL] enum vocabularies (provenance/validity/authority/kind/scope) not mechanically enforced
  - [OPTIONAL] X4 op content runtime bug: user.status = user.status.SUSPENDED (Status.ACTIVE.SUSPENDED AttributeError)
  - [OPTIONAL] R4 rationale asserts unverified property "fixture tests still pass" on mutated trees
  - [OPTIONAL] minor errata: lib.rs doc says 34 files (actual 35); X2 def-site reference convention implicit; no X1 textual cross-check
dispositions:
  - IMPORTANT → corrected (option a: full enumeration; X5 scope explicit; regression test added)
  - X4 bug → corrected in same batch; R4 wording → softened; X2 convention → documented in README
  - vocab enforcement / X1 cross-check → BACKLOG; mutated-tree harness → BACKLOG (OUT_OF_SCOPE for M2)
  - lib.rs doc → corrected
verdict: REPAIR_REQUIRED
final_gate: COMMIT_BLOCKED
review_closure: fresh re-review — cycle 2 against the full candidate diff from 80f354b (reviews/2026-09-08-M2-cycle2.md)
audit_evidence: >
  Contamination audit PASS on all seven questions (labels engine-independent;
  consequences unambiguous except the enumerated gap; no Trellis-favorable
  encoding; unchanged-value and absence cases present with concrete values;
  false-valid vs recomputation distinguishable; tests independently recompute
  from fixture text). 0 BLOCKER. Gates independently re-run green by reviewer
  (72 tests then, 18 unittest OK).

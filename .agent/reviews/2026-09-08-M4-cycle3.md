# Review 2026-09-08 M4 cycle 3 — post-repair full-diff review (fresh context)

scope: full M4 candidate diff from fac5f5f (cycle 3, per POST-REPAIR RULE after cycle 2's decode repairs)
spec_sections: §2.1, §10, §12, §19, §24, §63
findings:
  - [IMPORTANT] producer_dec silently decoded mistyped model field to None (same class as repaired cycle-2 cost finding); harness/version paths were already strict
dispositions:
  - IMPORTANT producer decode → corrected: strict per-field decode (harness/version must be strings; model must be null or string; anything else → Corrupt); regression corrupt_producer_model_is_explicit_not_silently_dropped
verdict: REPAIR_REQUIRED
final_gate: COMMIT_BLOCKED
review_closure: fresh re-review — cycle 4 against the full candidate diff from fac5f5f
audit_evidence: >
  Cycle-2 repairs verified in detail (evidence_dec Result-based decode,
  cost_field strict per-key decode; both regressions genuine and adversarial).
  Full decode-helper sweep tabulated (all helpers explicit Corrupt except the
  one producer gap = the finding). A-F PASS. Mechanical gates independently
  re-run green (123 tests then). Zero BLOCKER.

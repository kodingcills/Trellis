# Review 2026-09-14 M10 cycle 1 — full candidate review (fresh context: explore)

scope: Benchmarks A (oracle validity) + B (systems economics) — new test files benchmark_a.rs, benchmark_b.rs, tests/harness/mod.rs (extracted shared harness), committed results benchmarks/benchmark_a_results.txt + benchmark_b_results.txt, agent metadata (QUALITY_GATES.yaml M10 gate, DECISIONS.md M10 entries, runs/M10/ run log), redgreen.rs harness-consumption refactor. Zero production code changed.
spec_sections: §25 A, §25 B, §28 (success gates + kill criteria), §26, §25 fairness contract
findings:
  - [IMPORTANT] Run log and DECISIONS quoted an earlier draft run's numbers (C_validate 829µs / C_recompute 9570µs, 11.5×) while the committed artifact shows 1003µs / 8938µs = 8.9× — internally inconsistent documentation
dispositions:
  - IMPORTANT → corrected by controller post-review: run log and DECISIONS updated to quote the committed artifact as authoritative (8.9× headroom; attestation-append caveat ~7.9×). Both number sets agreed the premise HOLDS; only the docs were misaligned. Docs now match.
verdict: GREEN
final_gate: COMMIT_ALLOWED
review_closure: fresh review completed against the full candidate diff from 9d6cdfd; the single IMPORTANT was dispositioned (docs corrected to match the committed artifact)
audit_evidence: >
  Benchmark validity: oracle labels used only in the assertion layer
  (Catalog::load in harness seeding reads artifact metadata only;
  FixtureSource computes values independently of labels); full catalog
  covered (54 consequence rows = 9 mutations × 6 rows: R1, R2→R3, R4,
  X1, X2, X3, X4, X5); the zero-false-valid gate is real (11
  oracle-invalid rows measured, engine correctly non-VALID on all);
  harness independently constructs the semantic values (textual
  call-graph analysis), syntactic kinds delegate to the production
  SyntacticSource (same evaluator for seeding and measurement — by
  design the consistent evaluator).
  X5 path legitimacy: the benchmark's coverage hook reproduces the M7
  gate's production path exactly (apply ops → re-read tree → compute
  certificate from the POST-mutation tree → with_coverage transition);
  same mechanism as the M7 test, not label accommodation.
  Harness extraction: mechanical (verbatim region move from
  redgreen.rs to harness/mod.rs with pub-visibility added; new
  run_mutation_with + x5_coverage_hook are additive for benchmark
  reuse); all 15 redgreen tests green with unchanged names; 193/193
  workspace tests green.
  Economic premise honesty: committed numbers internally consistent
  (discovery 6µs + reevaluation 997µs = 1003µs vs reindex 8938µs =
  8.9×); fixture-scale caveat recorded; kill criterion not approached;
  C_validate formula measures discovery+reevaluation (attestation
  append measured separately and added as a caveat ~7.9×) — the
  omission is documented, conservative, and does not change the
  conclusion.
  Reproducibility: no network, fixed timestamps, deterministic A
  (54/54 both runs), B percentiles over fixed sample counts with
  run-to-run timing variance only; committed results produced by the
  committed harness (path-anchored output).
  Scope: zero production code changed (tests + benchmarks/ + control
  plane only); no M11/M12 code; layering respected (public APIs only).
  Mechanical gates green (193 passed / 0 failed, clippy -D warnings
  clean, fmt clean).

# Verdict

GREEN

# Findings

The one IMPORTANT (docs/artifact number misalignment) was corrected
post-review; the run log and DECISIONS now quote the committed artifact.
No remaining findings.

# Acceptance Criteria Audit

All M10_BENCHMARK_ACCEPTANCE criteria PASS — full catalog measured
(54/54), zero false-valid observed (11 oracle-invalid rows all
correct), recall/precision/abstention reported, Benchmark B measures
all required operations with p50/p95/p99 and frontier scaling,
economic premise measured and committed (8.9× headroom, HOLDS, honest
fixture-scale caveat), harnesses are rerunnable cargo tests committed
with the results, M0-M9 tests unchanged and green, zero production-code
changes.

# Architectural Invariant Audit

Oracle independence (§26): PASS. No benchmark-specific shortcuts in
production code (§25): PASS. Kill criteria recorded honestly (§28):
PASS. Append-only/epistemic invariants untouched by the harness: PASS.

# Test Adequacy

Benchmark A independently constructs every catalog scenario through the
shared harness and asserts every oracle row (54 rows, no mirroring —
the FixtureSource semantic evaluation is an independent textual
implementation). Benchmark B measures the production paths with fixed
sample counts. The gate is established by measurement, not assertion.

# Scope Audit

IN_SCOPE. Tests + benchmarks/ + control plane only; no production code;
no M11/M12 content.

# Final Gate Recommendation

COMMIT_ALLOWED

Rationale: zero BLOCKER; the single IMPORTANT was corrected (docs now
match the committed artifact); all gate criteria PASS; no
ARCHITECTURE_CONFLICT; mechanical gates green (193 passed / 0 failed,
clippy -D warnings clean, fmt clean).

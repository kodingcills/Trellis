# Review 2026-09-08 M5 cycle 3 — post-repair full-diff review (fresh context: oracle)

scope: full M5 candidate diff from 93ceb29 (cycle 3, per POST-REPAIR RULE after cycle-2 repairs)
spec_sections: §2.1, §7, §8, §9.3, §18, §19, §24, §26
findings:
  - [BLOCKER] rebinding detected once but rebound observation cannot be re-anchored (resolved path private to M3; record_observation accepted caller-supplied paths; subsequent modified-only change to the rebound unit omitted)
  - [BLOCKER] projection_descriptors discard the subject variant now in identity — legal cross-variant projections fail restart round-trip
  - [BLOCKER] observation identity bypasses canonical serialization (raw digest concat, no domain tag) and forged ids never verified at recording or load
  - [IMPORTANT] inventory-change rule broader than documented (non-Python add/remove blanket-selected unit-anchored kinds)
dispositions:
  - all 3 BLOCKERs → corrected (M3 resolve_symbol_unit/unit_path authoritative-resolution exposure; ObservedObservation + record_observation persisting the M3-resolved anchor; subject_variant column + exact reconstruction; canonical_observation_id in trellis-core verified at recording and load) with regressions
  - IMPORTANT → corrected (.py-only inventory rule) with regression
verdict: REPAIR_REQUIRED
final_gate: COMMIT_BLOCKED
review_closure: fresh re-review — cycle 4 against the full candidate diff from 93ceb29
audit_evidence: >
  A FAIL (sequential rebinding miss demonstrated), B PASS, C PASS, D PASS,
  E FAIL (descriptor round-trip + no authoritative new anchor). Cycle-2
  repairs verified (variant-in-identity PASS, reconstruction verification
  PASS, path-exact file_digest PASS, anchor conflicts PASS, release-mode
  kind checks PASS, battery PASS incl. X2 + lower bound, candidate-size
  evidence PASS). Mechanical gates green (147 tests then). No
  ARCHITECTURE_CONFLICT.

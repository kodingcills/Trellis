# Review 2026-09-08 M4 cycle 2 — post-repair full-diff review (fresh context)

scope: full M4 candidate diff from fac5f5f (per POST-REPAIR RULE after cycle-1 repairs)
spec_sections: §2.1, §10, §12, §19, §24, §63
findings:
  - [BLOCKER] evidence element decode silently drops malformed entries (filter_map over Option-returning evidence_dec) — corrupt state reinterpreted as valid; empirically confirmed by reviewer scratch harness (2 refs persisted, 1 loaded)
  - [IMPORTANT] cost_dec maps mistyped field values to None instead of Corrupt ("120" string → None) — silent restart drift; empirically confirmed
  - [OPTIONAL] conflicting artifact duplicate (same id, different content) silently first-wins via INSERT OR IGNORE — pre-existing, out of M4 contract (caller-supplied ids), asymmetry with attestation Constraint; on PK collision compare columns and return Constraint → BACKLOG
dispositions:
  - BLOCKER evidence decode → corrected: evidence_dec returns Result; collect::<Result<Vec<_>>>(); regression corrupt_evidence_element_is_explicit_not_silently_dropped
  - IMPORTANT cost decode → corrected: cost_field per-key Corrupt (missing/mistyped); regression corrupt_cost_field_is_explicit_not_silently_zeroed
  - OPTIONAL conflicting duplicate → BACKLOG
verdict: REPAIR_REQUIRED
final_gate: COMMIT_BLOCKED
review_closure: fresh re-review — cycle 3 against the full candidate diff from fac5f5f
audit_evidence: >
  All A-F questions PASS post-repair-1 (A untampered; D no sanctioned bypass
  to cas_objects rows — only the raw-transaction() escape hatch, accepted and
  recorded in DECISIONS). Cycle-1 repairs verified in detail (cost column,
  evidence/verifier round-trip, register_blob durability gate, dec_list,
  DECISIONS for traits/migrations deferral, collect_orphans GC). Mechanical
  gates independently re-run green (121 tests then).

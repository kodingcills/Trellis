# Review 2026-09-08 M5 cycle 1 — candidate discovery (fresh context: oracle)

scope: full M5 candidate diff from 93ceb29 — trellis-engine (observe/discovery/tests), store additive ENGINE_SCHEMA + accessors, ChangedSet Default, control plane
spec_sections: §2.1, §7, §8, §9.3, §18, §24, §26
findings:
  - [BLOCKER] symbol_locations PK=symbol + INSERT OR IGNORE silently discarded later recording-time paths (moved-symbol anchors lost; discovery then omits)
  - [BLOCKER] projection descriptors (kind/subject/scope) not persisted; recorded_from filter_map silently dropped unkeyed observations; restart test hardcoded keys
  - [BLOCKER] Imports discovery missed package __init__ units (module_file derivation duplicated M3 module identity and mapped users→users.py)
  - [BLOCKER] observe.rs invented a reduced projection identity (kind+subject, scope omitted) conflating same-subject/different-scope projections — second key representation violating M0 identity ownership
  - [IMPORTANT] over-approximations exceeded documentation (unanchored fired on empty changed set; undocumented catch-all kinds)
  - [IMPORTANT] oracle battery mostly hand-copied expectations (only R4 cross-checked catalog content)
dispositions:
  - all 4 BLOCKERs → corrected (composite-PK history; persisted descriptors + fail-closed loader; anchors recorded at observation time, module derivation removed; Projection::id() full canonical identity) with regression tests
  - both IMPORTANTs → corrected (unanchored/fallback = universe_changed, empty set selects nothing, documented; catalog-driven battery deriving changed sets from ops and expectations from affected_projections)
verdict: REPAIR_REQUIRED
final_gate: COMMIT_BLOCKED
review_closure: fresh re-review — cycle 2 against the full candidate diff from 93ceb29
audit_evidence: >
  A: sound classes enumerated, two end-to-end FAILs found (locations persistence,
  package-init imports). B: misses confirmed in those two classes. C:
  over-approximations undocumented. D: distinct concepts PASS. E: M6
  consumption blocked by missing durable descriptors. Mechanical gates green
  (141 tests). Reviewer noted implementation quality otherwise and confirmed
  no ARCHITECTURE_CONFLICT; defects repairable within M5.

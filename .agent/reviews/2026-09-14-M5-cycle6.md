# Review 2026-09-14 M5 cycle 6 — post-repair full-diff review (fresh context: explore)

scope: full M5 candidate diff from 93ceb29 — crates/trellis-engine (observe/discovery/tests), store additive ENGINE_SCHEMA + methods, trellis-core observation_id.rs + Projection identity methods, reconcile.rs ChangedSet Default, program/python.rs sources map + path-exact file_digest + resolve_symbol_unit, control-plane gate definitions, DECISIONS append
spec_sections: §2 principle 1 (correctness dominates reuse, over-approximation allowed), §7 (full canonical projection identity), §8 (fail-closed, uncertainty never empty), §9.3 (ProgramIndex boundary, M3-anchored resolution), §18 steps 1-3 (reverse indexes, candidate discovery), §19 (canonical serialization/identity, BLAKE3, crash-consistent publication, schema versioning), §24 (crate layering — engine depends only on store's typed API), §26 (oracle independence, M2 labels read-only)
findings: (none — zero BLOCKER, zero IMPORTANT)
dispositions: (none required — all three cycle-5 IMPORTANTs verified corrected in code)
verdict: GREEN
final_gate: COMMIT_ALLOWED
review_closure: fresh re-review completed against the full candidate diff from 93ceb29; zero BLOCKER/IMPORTANT findings, 8/8 acceptance criteria PASS
reviewer_vehicle_note: >
  Prior reviewer vehicles (oracle sync x2, deep sync x1, deep background x1,
  general background x1, ultrabrain background x1) failed without producing
  output due to harness model-resolution/timeout faults (REVIEW_INCOMPLETE
  states, protocol retry). This review was produced by a fresh explore-vehicle
  reviewer with full read access; it performed its own inspection of the
  complete diff, all untracked files, prior review records, and the frozen
  spec. Reviewer remained read-only (verified: working tree unchanged after
  review).
audit_evidence: >
  Cycle-5 repair claims ALL verified in code:
  1. Canonical decode centralized — decode_observation_row (store_impl.rs:306)
     shared by get_projection_observation (:1064) and all_projection_observations
     (:1245). Discriminating regressions: tampered_observation_digest_fails_closed_everywhere
     (persistence.rs:739) uses h(42) (valid-length different digest), so only
     re-derivation detects it — both direct and bulk paths fail Corrupt.
  2. Composite failure paths tested — composite_publication_rolls_back_on_final_insert_failure
     (persistence.rs:789): missing snapshot FK -> FK violation -> transaction()
     (:175) rollback -> descriptor+anchor+observation all absent post-failure.
     composite_anchor_conflict_rejected_and_unchanged (persistence.rs:827):
     same-id/different-anchor -> record_observation_record (:681) hits
     Some(p) => Err(Constraint(...)) conflict branch -> original anchor unchanged.
  3. DECISIONS.md append-only — file ends with 6 M5 entries (lines 45-50),
     last pre-existing entry is M3 (line 43), separator blank line preserved.
  False-negative analysis: 4 discovery classes cover ALL projection kinds;
  FileContent path-exact, unit-anchored (Definition/Signature/Imports) via
  anchor + inventory change (.py only), completeness-sensitive + fallback on
  any non-empty change, empty -> nothing. Renamed/moved symbols: composite-PK
  symbol_locations(symbol, source_path) preserves history. Deleted -> anchor
  unchanged, inventory rule fires. Rebind -> inventory change selects old+NRT
  anchors. Package __init__ tested (package_init_imports_are_anchored_to_init_py).
  sequential_rebind_reobservation_and_modified_only_discovery proves
  two-transition rebinding correctness.
  Layer verification: trellis-engine Cargo.toml declares trellis-store as
  regular dep; rusqlite is dev-only. Engine only calls Store's public methods
  (record_observation_record, put_symbol_location, all_projection_observations,
  projection_descriptor, observation_anchor). No direct SQLite table references.
  trellis-core std-only with blake3 (M1-authorized). No SCIP knowledge.
  Identity: canonical_observation_id (observation_id.rs:11) uses domain tag
  trellis.projection-observation.v1, field-framed, algorithm-tagged.
  Projection::id() (projection.rs:228) uses trellis.projection.v1,
  kind+variant+subject+property+scope. No paths/timestamps/machine state.
  ReevaluationCandidates uses BTreeSet -> canonical ordering.
  Epistemic hygiene: ReevaluationCandidates type strictly documents "may be
  affected" — no staleness/value-change concepts. Observe helpers return None
  for semantic kinds (tested: semantic_kinds_produce_no_observations). Kind
  mismatch returns explicit ObserveError::KindMismatch in all builds.
  Fail-closed: recorded_from_store fails Corrupt on missing
  descriptor/anchor/hash-mismatch. decode_observation_row fails Corrupt on
  non-deriving id. put_projection_observation and record_observation_record
  reject forged ids with Constraint. 6 fail-closed regressions.
  M2 oracle read-only: no_false_negatives_across_supported_m2_mutations uses
  Catalog::load() read-only, constructs projections from catalog keys, asserts
  must-not-miss.

# Verdict

GREEN

# Findings

None.

# Acceptance Criteria Audit

M5_IMPACT_ACCEPTANCE (QUALITY_GATES.yaml NODE_GATES):

1. no false negatives — PASS. Catalog-driven battery
   no_false_negatives_across_supported_m2_mutations (discovery.rs:379) iterates
   all supported mutations (R1-R4, X1-X5), derives changed sets from mutation
   ops, asserts every oracle-affected projection key is a candidate. Plus
   explicit class-level tests, rebinding scenario tests, empty-set test,
   non-Python inventory exclusion test.
2. over-approximation allowed and documented — PASS. discovery.rs:23-31
   documents four classes; conservative_fallback_kinds_match_any_file_change_and_are_documented verifies.
3. candidate != value-changed != artifact-stale distinct — PASS.
   ReevaluationCandidates is the only candidate type; lib.rs:5-14 and
   discovery.rs:33-36 state the M5 boundary; no staleness/value-equality
   concepts in engine.
4. deterministic candidate ordering; dedup — PASS. BTreeSet<ProjectionObservationId>
   canonical id sort + inherent dedup;
   multiple_changes_and_duplicates_deduplicate_deterministically (discovery.rs:193).
5. restart/reload identical results — PASS. discovery_survives_restart_via_persistence
   (discovery.rs:267); symbol_locations_preserve_history_across_reopen.
6. unsupported semantic knowledge never treated as known — PASS.
   semantic_kinds_are_recorded_data_not_synthesized (discovery.rs:549);
   observe helpers return Answer::Unsupported -> no observation produced;
   semantic_kinds_produce_no_observations.
7. M2 oracle labels untouched — PASS. Catalog::load() read-only; fixtures/oracle/
   unmodified in git status.
8. M0-M4 tests unchanged and green — PASS. Full workspace green; M4
   projection_observation_roundtrip fixture STRENGTHENED to canonical ids
   (strengthening, not weakening — documented in run log).

ROADMAP M5 acceptance:
- "R0 fixture: editing a file maps to exactly the projections touching its
  symbols (plus documented over-approximation)" — PASS. Catalog-driven battery
  covers R1-R4; MEASURE lines in run log with candidate-set sizes.
- "unrelated-file edits produce empty or minimal candidate sets" — PASS.
  unrelated_change_minimizes_candidates (discovery.rs:224).

# Architectural Invariant Audit

- No mutable epistemic fields on artifacts: PASS (engine creates no artifacts).
- Attestation history append-only: PASS (engine touches no attestations).
- Capture trust from derivation, never claimed: PASS (engine creates no derivations).
- Reuse floor Valid ∧ Authoritative ∧ ¬Unobserved: PASS (engine implements no reuse policy).
- No daemon/watcher/continuous dirty tracking: PASS (engine is pure-library; discovery lazy on demand).
- Projection kind/property consistency: PASS (record_observation_record uses Projection::new()).
- Canonical IDs algorithm-tagged, reproducible: PASS (canonical_observation_id + Projection::id(), BLAKE3, domain-tagged).
- trellis-core std-only (§24): PASS (blake3 authorized at M1; no new core deps).
- Engine -> store via typed public API only (§24): PASS (rusqlite dev-dependency only).
- No SCIP outside the adapter: PASS.
- Blob-first metadata-second (§19): PASS (engine writes metadata only; no blob ops).
- Schema versioning preserved: PASS (ENGINE_SCHEMA additive CREATE TABLE IF NOT EXISTS, idempotent on v1, no version bump per DECISIONS).

# Test Adequacy

The suite independently establishes the M5 contract. Catalog-driven battery is
structurally adversarial (reads M2 oracle as independent ground truth, derives
changed sets from mutation ops, asserts must-not-miss). Fail-closed tests prove
concrete attack scenarios (tampered digests, missing descriptor/anchor rows,
forged ids at ingress). Restart persistence equivalence tested with two full
open->load->discover comparisons. Rebind tested as a sequential two-transition
integration proving the full lifecycle. No weaknesses identified.

# Scope Audit

IN_SCOPE. All ROADMAP M5 deliverables present; no M6 leakage (no reevaluation,
no staleness, no value equality); no unrelated refactors; control-plane changes
(QUALITY_GATES, ROADMAP status, DECISIONS) scope-appropriate for node activation.

# Final Gate Recommendation

COMMIT_ALLOWED

Rationale: zero BLOCKER findings; zero un-dispositioned IMPORTANT findings; all
8 M5_IMPACT_ACCEPTANCE criteria PASS; both ROADMAP M5 acceptance items PASS; all
architectural invariants PASS; mechanical gates green (controller-run:
cargo test --workspace 157 passed / 0 failed, clippy -D warnings clean, fmt
clean — count corrected from the run log's 177 typo, whose own components
6+40+6+22+14+22+27+20 sum to 157); no ARCHITECTURE_CONFLICT; IN_SCOPE.

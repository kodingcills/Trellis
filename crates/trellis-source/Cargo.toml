[package]
name = "trellis-source"
description = "Deterministic source state: working-tree manifests, lazy reconciliation, environment fingerprints."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[dependencies]
trellis-core = { path = "../trellis-core" }

[dev-dependencies]
tempfile = "3"

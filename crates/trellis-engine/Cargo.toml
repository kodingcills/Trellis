[package]
name = "trellis-engine"
description = "Dependency engine: projection observation recording and conservative dirty-candidate discovery (spec §18 steps 1-3)."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[dependencies]
trellis-core = { path = "../trellis-core" }
trellis-source = { path = "../trellis-source" }
trellis-program = { path = "../trellis-program" }
trellis-store = { path = "../trellis-store" }

[dev-dependencies]
tempfile = "3"
trellis-oracle = { path = "../trellis-oracle" }
trellis-cas = { path = "../trellis-cas" }
rusqlite = { version = "0.37", features = ["bundled"] }
